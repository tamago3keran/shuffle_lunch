import React from 'react';
import ReactDOM from 'react-dom';
import Sidebar from '../../../components/Sidebar';

ReactDOM.render(
  <Sidebar activeList="users" />,
  document.getElementById('js-user_matching_score_index'),
);